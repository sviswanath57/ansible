- name: checking data types
  hosts: localhost
  vars:
    Name: "S Viswanath"
    skill:
    - devops
    - Windows
    - VMware
    Experience:
      devops: 2
      windows: 10
      vmware: 10
    Bool: true
  
  tasks:
    ansible.builtin.debug:
      msg: "{{Name}}"

  tasks:
    ansible.builtin.debug:
      msg: "{{skill}}"

  tasks:
    ansible.builtin.debug:
      msg: "{{Experience}}"
  
  tasks:
    ansible.builtin.debug:
      msg: "{{Bool}}"